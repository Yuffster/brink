// Require the main server-side Brink file; make sure to add Brink to the 
// global scope.

// Assets = require('assets');


// Require all the app JS files with Brink defined globally.

//assetServer.listen(app);

// Asset Server:
	// Pack up all the client-side API files in an adorable little closure.
	// Start the asset server, which will handle serving all the client-side JS.
	// Listen on app.

// Reactor:
	// Wait for Router to be like, "Hey, I need to render this screen with this data."
	// Bind an event to Brink's global data handler to be notified when anything changes.

// Messenger:
	// Initate the messenger and bind to Brink for handling.

// Socket:
	// Bind Socket.io to the server so it can serve up its files. 

// Router:
	// Initiate the router and bind it to the app.
	// Request comes in:
		// Router looks for a matching path in the routes table.
		// Router handles a render call to take a screen and add a template.